# Plan: UI Parity - Library Grid Sorting & Filter Controls

- **Objective**: Improve UI parity of the Library Grid header controls.
- **Specific Tasks**:
  1. Inspect `frontend/index.html` and relevant Vue components to find the sorting and filter dropdown elements.
  2. Audit `frontend/css/layout.css` to check for grid alignment styles.
  3. Compare with project screenshots to identify exact spacing and styling discrepancies in dropdowns and result counts.
  4. Implement CSS adjustments to mirror the original Audiobookshelf grid header.
  5. Verify that result count labels are positioned correctly according to visual audit.
