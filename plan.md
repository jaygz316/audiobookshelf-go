# Plan: Audiobookshelf Go Port UI Alignment

- The goal is to refine action button container spacing and audit settings screen header dropdown colors.
- Current state: Header action buttons and user pill are defined in `layout.css`. Settings header dropdowns have reported color issues.
- Plan:
    1. Inspect action button spacing in `layout.css`.
    2. Identify settings screen header dropdown CSS in `components.css` or `layout.css`.
    3. Adjust CSS for better spacing and color consistency.
    4. Verify changes in the UI.
